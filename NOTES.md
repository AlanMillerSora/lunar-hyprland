# TODO — система (19.09.2026)

Запросы пользователя. Активный цикл: **0.1.3 → 0.1.4** («Lunar-launcher»).
Правило работы: по одному пункту «сделал → проверил → очистил контекст → дальше».

## 1. Lunar-launcher — общий лаунчер системы
- [x] Идея: веб-интерфейс + CSS (тёмная тема Lunar Eclipse), сервер Python stdlib.
- [ ] Автозапуск на 1-м рабочем столе при включении системы.
- [ ] Интерфейс: сетка приложений + системные контролы (громкость, яркость,
      Wi-Fi, медиа, питание) — минималистично и красиво.
- [ ] Горячая клавиша показать/спрятать (SUPER+G).
- [ ] Правило окна в Hyprland: плавающее, по центру, без рамок, скругление.

## 2. Прозрачность всей системы (ползунок в лаунчере)
- [ ] Ползунок → `hyprctl keyword decoration:active_opacity / inactive_opacity`.
- [ ] Сохранение значения (файл) + применение при старте сессии.

## 3. Плавность системы в целом
- [ ] Настроить анимации Hyprland (кривые/скорости), polkit-агент для питания.

## 4. Анимации перехода между рабочими столами
- [ ] `workspaces` → более заметный стиль (slidefadediagonal, curve eclipse).

## 5. Wi-Fi «умирает» — починка
- [x] Диагноз: профиля не было; NM рандомизировал MAC при сканировании →
      роутер рвал ассоциацию. Лечение: профиль + постоянный MAC + no power-save.
- [x] Профиль `Xiaomi-45` создан: autoconnect, cloned-mac=permanent,
      mac-randomization=never, powersave=disable (живая система).
- [ ] Сторож-авточинилка (systemd --user): если сеть отвалилась — поднять заново.
- [ ] Проверить, что пароль Wi-Fi реально записан (подключение пережило все рестарты).

## 6. Версия и релиз
- [ ] `VERSION` 0.1.3 → 0.1.4 + запись в `CHANGELOG.md`.
- [ ] Пуш на github (токен в истории сессий или новый из логов).

## Примечания
- Установщик (`install.sh`) копирует лаунчер и сторож; `setup.sh` — пакеты.
- `sudo -n` недоступен: пароль `ula662848` (даёт пользователь). Brightness — через
  `sudo -A` (eclipse-askpass.py) с фолбэком на прямой write, если получится.
- Быстрая проверка: `hyprctl configerrors`; живой статус окон — `hyprctl clients -j`.