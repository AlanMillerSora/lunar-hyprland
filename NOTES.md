# TODO — система (19.09.2026)

Запросы пользователя. Активный цикл: **0.1.3 → 0.1.4** («Lunar-launcher»).
Правило работы: по одному пункту «сделал → проверил → очистил контекст → дальше».

## 1. Lunar-launcher — общий лаунчер системы ✅
- [x] Идея: веб-интерфейс + CSS (тёмная тема Lunar Eclipse), сервер Python stdlib.
- [x] Автозапуск на 1-м рабочем столе при включении системы (hyprland.start).
- [x] Интерфейс: сетка приложений + системные контролы (громкость, яркость,
      Wi-Fi, медиа, питание) — минималистично и красиво.
- [x] Горячая клавиша показать/спрятать (SUPER+G).
- [x] Правило окна: плавающее, по центру, без рамок, скругление 16,
      no_initial_focus (не ворует фокус при входе).

## 2. Прозрачность всей системы (ползунок в лаунчере) ✅
- [x] Ползунок → `hyprctl eval` → `decoration:active_opacity / inactive_opacity`.
- [x] Сохранение в `~/.config/lunar/transparency` + применение при старте
      (`eclipse-transparency.sh`).

## 3. Плавность системы в целом ✅
- [x] `animate_manual_resizes` + `animate_mouse_windowdragging`, убран логотип,
      добавлен polkit-агент (работают действия питания).

## 4. Анимации перехода между рабочими столами ✅
- [x] `workspaces` → `slidefadediagonal` на кривой «затмение», скорости подкручены.

## 5. Wi-Fi «умирает» — починка ✅
- [x] Диагноз: профиля не было; NM рандомизировал MAC при сканировании →
      роутер рвал ассоциацию.
- [x] Профиль `Xiaomi-45`: autoconnect, cloned-mac=permanent,
      mac-randomization=never, powersave=disable (сеть стабильно держится).
- [x] Сторож `eclipse-wifi-guard.py` (systemd --user, включён) — поднимет сеть,
      если отвалится.
- [x] Пароль в профиле реально записан (подключение переживает рестарты).

## 6. Версия и релиз ✅
- [x] `VERSION` 0.1.3 → 0.1.4 + запись в `CHANGELOG.md` + README.
- [x] Пуш на github (main = 9a3b463).

## Примечания
- Установщик (`install.sh`) тянет лаунчер, скрипты и юнит сторожа; `setup.sh` — пакеты.
- `sudo -n` недоступен: яркость — через `sudo -A` (eclipse-askpass.py), фолбэк
  на прямой write. Пароль: `ula662848` (даёт пользователь).
- Быстрая проверка: `hyprctl configerrors`; живой статус окон — `hyprctl clients -j`.
- Готча 0.56: `hyprctl dispatch` — это Lua; закрывать окно
  `hl.dsp.window.close({ window = 'address:...' })`.