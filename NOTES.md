# TODO — система (18.09.2026)

Запросы пользователя по системе. Обои — update 0.1.2, полировка — update 0.1.3.

## 1. Waybar (верхняя панель)
- [x] Индикатор «заполнение затмения» переделан: тонкая полоса 10 делений
      + глиф фазы + %, на полном затмении красный. (`eclipse-pbar.sh`)
- [x] Громкость — клик → `pavucontrol`, правый клик → mute, колесо ±5%.
- [x] Wi-Fi — клик → меню сетей (wofi+nmcli), правый клик → nm-connection-editor.
- [x] Календарь — клик по часам → GTK-календарь, клик по дню копирует дату.
- [x] Внутри всё рабочее, не заглушки.

## 2. Горячие клавиши
- [x] `SUPER` + `/` → оверлей горячих клавиш (`eclipse-cheatsheet.py`).
- [x] `SUPER` + `E` → Dolphin. Медиа-клавиши через `wpctl`.

## 3. Терминал
- [x] kitty: курсор-beam, паддинги, вкладки при ≥2, чистое закрытие.
- [x] fastfetch-конфиг + логотип-полузатмение, PS1/алиасы в `shell/lunar.bash`.
- [ ] **Пакет `fastfetch` не установлен** (`sudo` требует пароль) — поставить
      `sudo pacman -S fastfetch`. Конфиг уже лежит в `~/.config/fastfetch/`.

## 4. Лаунчер и файловый менеджер
- [x] wofi — центрированный, скругления, тень, аккуратный ввод.
- [x] Dolphin — схема `Lunar Eclipse`, kdeglobals + dolphinrc.

## Примечания
- Установщик (`install.sh`) тянет всё новое; `setup.sh` добавил `fastfetch`.
- `sudo -n` недоступен — новые пакеты ставит пользователь.
